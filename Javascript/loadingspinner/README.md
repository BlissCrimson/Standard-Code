# loading spinner

Ein loading spinner wird genutz um dem User mit einer Animation die Ladezeit anzuzeigen.
Dabei gibt es unzählige varianten wie man das umsetzten kann.


## JavaScript

Im JavaScript gibt man die Logik ein damit der loading spinner angezeigt wird.

[Hier](loadingspinner.js) finden sie zwei codesnippets um den loadingspinner anzuzeigen und auszublenden.


## CSS

Natürlich muss der loading spinner auch gestylt werden.

[Hier](loadingspinner.css) finden sie eine Style, eines loading spinner. Dieser wird hier über die ganzer Webseite angezeigt.
Da wo er verwendet wurde, wird er mit Javascript, über die anderen Elemente eingeblendet und nach einer vorgegebenen Zeit wird ausgeblendet.